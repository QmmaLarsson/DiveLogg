@model DiveLogg.ViewModels.DiveEditViewModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Redigera</h1>

<h4>Dyk</h4>
<div>
    <div>
        <form asp-action="Edit" method="post">

            <div>
                <label asp-for="DiveDate"></label>
                <input asp-for="DiveDate" />
                <span asp-validation-for="DiveDate"></span>
            </div>

            <!-- Dykledare -->
            <div>
                <label asp-for="DiveLeaderId"></label>
                <select asp-for="DiveLeaderId" asp-items="Model.DiveLeaders"></select>
            </div>

            <!-- Dykare -->
            <div>
                <label asp-for="DiverId"></label>
                <select asp-for="DiverId" asp-items="Model.Divers"></select>
            </div>

            <!-- Dykskötare -->
            <div>
                <label asp-for="DiveSupportId">Dykskötare: </label>
                <select asp-for="DiveSupportId" asp-items="Model.DiveSupports">
                    <option value=""></option>
                </select>
            </div>

            <div>
                <label asp-for="Depth"></label>
                <input asp-for="Depth" />
            </div>

            <div>
                <label asp-for="DiveTime"></label>
                <input asp-for="DiveTime" />
            </div>

            <div>
                <label asp-for="ExposureTime"></label>
                <input asp-for="ExposureTime" />
            </div>

            <div>
                <label asp-for="NitrogenLoad"></label>
                <input asp-for="NitrogenLoad" />
            </div>

            <div>
                <label asp-for="Latitude"></label>
                <input asp-for="Latitude" />
            </div>

            <div>
                <label asp-for="Longitude"></label>
                <input asp-for="Longitude" />
            </div>

            <div>
                <label asp-for="LocationName"></label>
                <input asp-for="LocationName" />
            </div>

            <div>
                <label asp-for="Notes"></label>
                <input asp-for="Notes" />
            </div>

            <div>
                <input type="submit" value="Save" />
            </div>

            <!-- Felmeddelanden -->
            <div asp-validation-summary="All"></div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Tillbaka till lista</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}